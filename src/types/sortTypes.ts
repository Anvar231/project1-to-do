export enum SortByDate {
    new = "new",
    old = "old"
}

export enum SortByCompleted {
    all = "all",
    completed = "completed",
    nonCompleted = "non-completed"
}

export enum LimitCount {
    five = 5,
    ten = 10,
    fifteen = 15
}